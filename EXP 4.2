// App.jsx
import React from "react";

class Person {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }
  displayInfo() {
    return Name: ${this.name}, Age: ${this.age};
  }
}

class Student extends Person {
  constructor(name, age, course) {
    super(name, age);
    this.course = course;
  }
  displayInfo() {
    return ${super.displayInfo()}, Course: ${this.course};
  }
}

class Teacher extends Person {
  constructor(name, age, department) {
    super(name, age);
    this.department = department;
  }
  displayInfo() {
    return ${super.displayInfo()}, Department: ${this.department};
  }
}

export default function App() {
  const student1 = new Student("Alice", 21, "Computer Science");
  const teacher1 = new Teacher("Mr. Brown", 45, "Mathematics");

  const cardStyle = {
    backgroundColor: "#f5f5f5",
    borderRadius: "12px",
    boxShadow: "0 4px 10px rgba(0,0,0,0.1)",
    padding: "20px",
    margin: "15px",
    minWidth: "250px",
    maxWidth: "300px",
    flex: "1 1 250px", // grow & shrink nicely
    transition: "transform 0.2s, box-shadow 0.2s",
  };

  const cardHover = (e) => {
    e.currentTarget.style.transform = "translateY(-5px)";
    e.currentTarget.style.boxShadow = "0 8px 20px rgba(0,0,0,0.2)";
  };

  const cardLeave = (e) => {
    e.currentTarget.style.transform = "translateY(0)";
    e.currentTarget.style.boxShadow = "0 4px 10px rgba(0,0,0,0.1)";
  };

  return (
    <div
      style={{
        backgroundColor: "#fff",
        color: "#333",
        minHeight: "100vh",
        width: "100vw", // take full width
        margin: 0,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        fontFamily: "'Segoe UI', Tahoma, Geneva, Verdana, sans-serif",
        textAlign: "center",
        padding: "40px 20px",
        boxSizing: "border-box",
      }}
    >
      <h1 style={{ marginBottom: "30px" }}>Person Class Hierarchy Demo</h1>
      <div
        style={{
          display: "flex",
          flexWrap: "wrap",
          justifyContent: "center", // center on small screens
          gap: "20px",
          maxWidth: "1000px", // keeps content neat on big screens
          width: "100%",
        }}
      >
        <div
          style={cardStyle}
          onMouseEnter={cardHover}
          onMouseLeave={cardLeave}
        >
          <h2 style={{ color: "#4A90E2" }}>Student</h2>
          <p>{student1.displayInfo()}</p>
        </div>
        <div
          style={cardStyle}
          onMouseEnter={cardHover}
          onMouseLeave={cardLeave}
        >
          <h2 style={{ color: "#E94E77" }}>Teacher</h2>
          <p>{teacher1.displayInfo()}</p>
        </div>
      </div>
    </div>
  );
}
